# 项目完整性检查清单

## 📦 新电脑部署前检查

使用此清单确保所有必需文件都已包含，可以顺利在新电脑上部署。

---

## ✅ 必需文件检查

### 1. 解决方案文件

- [ ] `AuditFlow.sln` - Visual Studio 解决方案文件

### 2. 后端项目文件

#### AuditFlow.Shared (共享类库)
- [ ] `AuditFlow.Shared.csproj`
- [ ] `Data/AuditFlowDbContext.cs`
- [ ] `Data/CS_SimulatorDbContext.cs`
- [ ] `Entities/HardwareAsset.cs`
- [ ] `Entities/CVEThreat.cs`
- [ ] `DTOs/AuditSummaryDto.cs`
- [ ] `DTOs/ThreatSummaryDto.cs`

#### AuditFlow.Simulator (威胁数据服务)
- [ ] `AuditFlow.Simulator.csproj`
- [ ] `Program.cs`
- [ ] `appsettings.json`
- [ ] `Properties/launchSettings.json`

#### AuditFlow.Engine (审计引擎服务)
- [ ] `AuditFlow.Engine.csproj`
- [ ] `Program.cs`
- [ ] `appsettings.json`
- [ ] `Properties/launchSettings.json`

### 3. 前端项目文件

#### AuditFlow.UI
- [ ] `package.json`
- [ ] `package-lock.json` (可选，但建议保留)
- [ ] `vite.config.ts`
- [ ] `tsconfig.json`
- [ ] `tsconfig.app.json`
- [ ] `tsconfig.node.json`
- [ ] `tailwind.config.js`
- [ ] `postcss.config.cjs`
- [ ] `index.html`
- [ ] `src/App.tsx`
- [ ] `src/main.tsx`
- [ ] `src/types.ts`
- [ ] `src/index.css`
- [ ] `.eslintrc.js` 或 `eslint.config.js`

### 4. 配置文件

- [ ] `README.md` (项目说明)
- [ ] `DevGuide.md` (开发指南)
- [ ] `迁移部署指南.md` (迁移步骤)

### 5. 脚本文件（可选但推荐）

- [ ] `start-backend.ps1` (Windows 启动脚本)
- [ ] `start-backend.sh` (Linux/Mac 启动脚本)
- [ ] `重置数据库.ps1` (数据库重置脚本)

---

## ❌ 不需要的文件

以下文件**不需要**复制到新电脑，会在运行 `dotnet restore` 和 `npm install` 时自动生成：

- [ ] `bin/` 目录（编译输出）
- [ ] `obj/` 目录（临时文件）
- [ ] `node_modules/` 目录（npm 依赖）
- [ ] `dist/` 目录（前端构建输出）
- [ ] `*.user` 文件（用户特定设置）
- [ ] `.vs/` 目录（Visual Studio 缓存）

---

## 🔍 完整性验证命令

在新电脑上使用以下命令验证：

```powershell
# 1. 检查解决方案文件
Test-Path AuditFlow.sln

# 2. 检查所有 .csproj 文件
Get-ChildItem -Recurse -Filter "*.csproj" | Select-Object FullName

# 3. 检查前端关键文件
Test-Path AuditFlow.UI\package.json
Test-Path AuditFlow.UI\src\App.tsx
Test-Path AuditFlow.UI\vite.config.ts

# 4. 检查配置文件
Test-Path AuditFlow.Simulator\appsettings.json
Test-Path AuditFlow.Engine\appsettings.json
```

---

## 📋 部署前清单

在新电脑上部署前，确认：

### 环境准备
- [ ] .NET 9 SDK 已安装
- [ ] Node.js 18+ 已安装
- [ ] SQL Server LocalDB 已安装（或 SQL Server Express）

### 文件准备
- [ ] 所有必需文件已复制到新电脑
- [ ] 项目目录结构完整
- [ ] 没有遗漏关键文件

### 验证步骤
- [ ] 运行 `dotnet restore` 成功
- [ ] 运行 `npm install` 成功
- [ ] 运行 `dotnet build` 成功
- [ ] 所有服务可以启动

---

## 🚀 快速检查脚本

创建 `check-completeness.ps1`:

```powershell
Write-Host "=== Project Completeness Check ===" -ForegroundColor Cyan

$missing = @()

# Check solution
if (-not (Test-Path "AuditFlow.sln")) {
    $missing += "AuditFlow.sln"
}

# Check projects
$projects = @("AuditFlow.Shared", "AuditFlow.Simulator", "AuditFlow.Engine")
foreach ($project in $projects) {
    if (-not (Test-Path "$project\$project.csproj")) {
        $missing += "$project\$project.csproj"
    }
    if (-not (Test-Path "$project\Program.cs") -and $project -ne "AuditFlow.Shared") {
        $missing += "$project\Program.cs"
    }
}

# Check frontend
if (-not (Test-Path "AuditFlow.UI\package.json")) {
    $missing += "AuditFlow.UI\package.json"
}
if (-not (Test-Path "AuditFlow.UI\src\App.tsx")) {
    $missing += "AuditFlow.UI\src\App.tsx"
}

# Report
if ($missing.Count -eq 0) {
    Write-Host "`n✓ All required files present!" -ForegroundColor Green
} else {
    Write-Host "`n✗ Missing files:" -ForegroundColor Red
    $missing | ForEach-Object { Write-Host "  - $_" -ForegroundColor Yellow }
}
```

---

## ✅ 当前项目状态

根据检查，当前项目**已具备完整条件**，可以部署到新电脑。

**包含的文件**:
- ✅ 所有 .csproj 文件
- ✅ 所有 Program.cs 文件
- ✅ 所有配置文件 (appsettings.json)
- ✅ 前端所有必需文件
- ✅ 解决方案文件 (AuditFlow.sln)
- ✅ 文档和脚本

**遵循迁移部署指南.md 即可完成部署** ✅

---

**最后更新**: 2026-01-20
