# 项目打包指南

## ✅ 打包前检查

### 必需文件检查

已确认所有必需文件都在：

- ✅ `AuditFlow.sln` - 解决方案文件
- ✅ `AuditFlow.Shared/` - 共享类库（所有 .cs 文件）
- ✅ `AuditFlow.Simulator/` - Simulator 服务（Program.cs, appsettings.json）
- ✅ `AuditFlow.Engine/` - Engine 服务（Program.cs, appsettings.json）
- ✅ `AuditFlow.UI/` - 前端项目（src/, package.json, vite.config.ts）

---

## 📦 打包方式

### 方式 1: 压缩包打包（推荐）

#### 需要包含的文件

✅ **包含这些**:

```
AuditFlow/
├── AuditFlow.sln
├── AuditFlow.Shared/           # 整个文件夹（不包括 bin/obj）
├── AuditFlow.Simulator/        # 整个文件夹（不包括 bin/obj）
├── AuditFlow.Engine/           # 整个文件夹（不包括 bin/obj）
├── AuditFlow.UI/               # 整个文件夹（不包括 node_modules/dist）
├── DevGuide.md
├── 迁移部署指南.md
├── 代码审查报告.md
└── 其他文档文件（.md）
```

#### 不需要包含的文件

❌ **排除这些**（会在新电脑上重新生成）:

```
❌ bin/          # 编译输出目录
❌ obj/          # 编译临时文件
❌ node_modules/ # npm 依赖（会重新安装）
❌ dist/         # 前端构建输出（会重新构建）
❌ *.user        # 用户特定文件
❌ .vs/          # Visual Studio 缓存
```

---

### 方式 2: Git 打包（如果有 Git 仓库）

如果项目在 Git 仓库中：

```powershell
# 创建归档
git archive -o AuditFlow.zip HEAD

# 或使用 tar
git archive -o AuditFlow.tar.gz HEAD
```

**优点**:
- 自动排除 .gitignore 中的文件
- 只包含版本控制的文件
- 文件更小

---

### 方式 3: 手动选择文件打包

#### PowerShell 脚本（创建 `package-project.ps1`）

```powershell
# package-project.ps1
$packageName = "AuditFlow-$(Get-Date -Format 'yyyyMMdd-HHmmss')"
$tempDir = ".\$packageName"

# 创建临时目录
New-Item -ItemType Directory -Path $tempDir -Force | Out-Null

Write-Host "=== Packaging AuditFlow Project ===" -ForegroundColor Cyan

# 复制解决方案文件
Copy-Item "AuditFlow.sln" -Destination "$tempDir\" -Force

# 复制后端项目（排除 bin/obj）
$projects = @("AuditFlow.Shared", "AuditFlow.Simulator", "AuditFlow.Engine")
foreach ($project in $projects) {
    Write-Host "Copying $project..." -ForegroundColor Yellow
    New-Item -ItemType Directory -Path "$tempDir\$project" -Force | Out-Null
    
    # 复制所有文件，排除 bin 和 obj
    Get-ChildItem -Path $project -Recurse -Exclude "bin","obj","*.user" | 
        Copy-Item -Destination { $_.FullName -replace [regex]::Escape($project), "$tempDir\$project" } -Recurse -Force
}

# 复制前端项目（排除 node_modules 和 dist）
Write-Host "Copying AuditFlow.UI..." -ForegroundColor Yellow
New-Item -ItemType Directory -Path "$tempDir\AuditFlow.UI" -Force | Out-Null
Get-ChildItem -Path "AuditFlow.UI" -Recurse -Exclude "node_modules","dist",".vs","*.user" | 
    Copy-Item -Destination { $_.FullName -replace [regex]::Escape("AuditFlow.UI"), "$tempDir\AuditFlow.UI" } -Recurse -Force

# 复制文档
Write-Host "Copying documentation..." -ForegroundColor Yellow
Copy-Item "*.md" -Destination "$tempDir\" -Force -Exclude "README.md"
Copy-Item "*.ps1" -Destination "$tempDir\" -Force -ErrorAction SilentlyContinue

# 创建压缩包
Write-Host "`nCreating ZIP archive..." -ForegroundColor Yellow
Compress-Archive -Path "$tempDir\*" -DestinationPath "$packageName.zip" -Force

# 清理临时目录
Remove-Item -Path $tempDir -Recurse -Force

Write-Host "`n✓ Package created: $packageName.zip" -ForegroundColor Green
Write-Host "`nPackage size:" -ForegroundColor Cyan
$size = (Get-Item "$packageName.zip").Length / 1MB
Write-Host "  $([math]::Round($size, 2)) MB" -ForegroundColor White
```

**使用方法**:
```powershell
.\package-project.ps1
```

---

## 📋 打包检查清单

打包前确认：

- [ ] 所有源代码文件都在（.cs, .tsx, .ts）
- [ ] 所有配置文件都在（.json, .config）
- [ ] 解决方案文件存在（.sln）
- [ ] 项目文件都在（.csproj）
- [ ] 文档文件都在（.md）
- [ ] 已排除 bin/ 目录
- [ ] 已排除 obj/ 目录
- [ ] 已排除 node_modules/ 目录
- [ ] 已排除 dist/ 目录

---

## 🚀 快速打包命令

### 简单方式：手动压缩（推荐新手）

1. **在文件资源管理器中**:
   - 右键点击项目根目录
   - 选择"发送到" → "压缩(zipped)文件夹"

2. **手动排除**:
   - 打开压缩包
   - 删除 `bin/`、`obj/`、`node_modules/`、`dist/` 文件夹
   - 保存

### 使用 PowerShell（推荐）

```powershell
# 安装脚本后运行
.\package-project.ps1
```

---

## 📦 打包后验证

解压到临时目录验证：

```powershell
# 1. 解压
Expand-Archive -Path "AuditFlow.zip" -DestinationPath ".\temp-check"

# 2. 检查关键文件
cd temp-check
Test-Path "AuditFlow.sln"
Test-Path "AuditFlow.Shared\AuditFlow.Shared.csproj"
Test-Path "AuditFlow.Simulator\Program.cs"
Test-Path "AuditFlow.Engine\Program.cs"
Test-Path "AuditFlow.UI\package.json"
Test-Path "AuditFlow.UI\src\App.tsx"

# 3. 检查排除的文件
if (Test-Path "AuditFlow.UI\node_modules") {
    Write-Host "WARNING: node_modules should not be included" -ForegroundColor Yellow
}

# 4. 清理
cd ..
Remove-Item -Path "temp-check" -Recurse -Force
```

---

## ✅ 打包确认

### 当前状态

✅ **项目文件完整，可以打包**

**建议打包方式**:
1. **手动压缩**（最简单）
2. **使用 PowerShell 脚本**（自动化）

**打包后大小预估**: 约 2-5 MB（不包含依赖）

---

## 📝 打包后部署

打包完成后：

1. **复制压缩包**到新电脑
2. **解压**到目标位置
3. **按照 `迁移部署指南.md`** 部署

---

**最后更新**: 2026-01-20
