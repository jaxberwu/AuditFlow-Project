# AuditFlow 系统迁移部署指南

## 📋 目录

1. [环境准备](#环境准备)
2. [代码迁移](#代码迁移)
3. [依赖安装](#依赖安装)
4. [数据库配置](#数据库配置)
5. [服务启动](#服务启动)
6. [验证测试](#验证测试)
7. [常见问题](#常见问题)

---

## 🔧 环境准备

### 步骤 1: 检查系统要求

**操作系统**: Windows 10/11 或 Windows Server

**必需软件检查**:

```powershell
# 检查 .NET SDK
dotnet --version
# 应该显示: 9.x.x

# 检查 Node.js
node --version
# 应该显示: v18.x.x 或更高

# 检查 SQL Server LocalDB
sqllocaldb info mssqllocaldb
# 应该显示 LocalDB 信息
```

### 步骤 2: 安装必需软件

#### 2.1 安装 .NET 9 SDK

1. **下载 .NET 9 SDK**
   - 访问: https://dotnet.microsoft.com/download/dotnet/9.0
   - 下载: **.NET SDK 9.0** (不是 Runtime)
   - 选择 Windows x64 版本

2. **安装**
   - 运行下载的安装程序
   - 按照向导完成安装
   - 选择默认选项即可

3. **验证安装**
   ```powershell
   dotnet --version
   # 应该显示: 9.0.x
   ```

#### 2.2 安装 Node.js

1. **下载 Node.js**
   - 访问: https://nodejs.org/
   - 下载: **LTS 版本** (推荐 v18 或 v20)
   - 选择 Windows Installer (.msi)

2. **安装**
   - 运行安装程序
   - 选择默认选项
   - 确保勾选 "Add to PATH"

3. **验证安装**
   ```powershell
   node --version
   # 应该显示: v18.x.x 或更高
   
   npm --version
   # 应该显示: 9.x.x 或更高
   ```

#### 2.3 安装 SQL Server LocalDB

**选项 A: 如果已安装 Visual Studio**
- LocalDB 通常已随 Visual Studio 安装
- 跳过此步骤

**选项 B: 单独安装 LocalDB**

1. **下载 SQL Server Express**
   - 访问: https://www.microsoft.com/sql-server/sql-server-downloads
   - 下载: **SQL Server Express** (包含 LocalDB)
   - 选择 "Express" 版本

2. **安装**
   - 运行安装程序
   - 选择 "基本" 安装类型
   - 完成安装

3. **验证安装**
   ```powershell
   sqllocaldb info mssqllocaldb
   # 应该显示 LocalDB 实例信息
   ```

---

## 📦 代码迁移

### 步骤 3: 复制项目文件

#### 3.1 准备源文件

在**原电脑**上，确保项目完整：

```
AuditFlow/
├── AuditFlow.Shared/
├── AuditFlow.Simulator/
├── AuditFlow.Engine/
├── AuditFlow.UI/
├── AuditFlow.sln
└── 其他配置文件
```

#### 3.2 复制到新电脑

**方式 A: 使用 U盘/移动硬盘**

1. 将整个 `AuditFlow` 文件夹复制到 U盘
2. 在新电脑上，复制到目标位置（如 `D:\Projects\AuditFlow`）

**方式 B: 使用 Git（推荐）**

如果项目在 Git 仓库中：

```powershell
# 在新电脑上
cd D:\Projects
git clone <your-repository-url> AuditFlow
cd AuditFlow
```

**方式 C: 压缩包传输**

1. 在原电脑上压缩整个项目文件夹
2. 传输到新电脑（网络/云盘/邮件）
3. 解压到目标位置

### 步骤 4: 验证文件完整性

在新电脑上检查：

```powershell
cd D:\Projects\AuditFlow  # 或你的项目路径

# 检查关键文件
dir AuditFlow.sln
dir AuditFlow.Shared\AuditFlow.Shared.csproj
dir AuditFlow.Simulator\AuditFlow.Simulator.csproj
dir AuditFlow.Engine\AuditFlow.Engine.csproj
dir AuditFlow.UI\package.json
```

---

## 📥 依赖安装

### 步骤 5: 还原 .NET 依赖

```powershell
cd D:\Projects\AuditFlow

# 还原所有 NuGet 包
dotnet restore

# 或单独还原每个项目
dotnet restore AuditFlow.Shared
dotnet restore AuditFlow.Simulator
dotnet restore AuditFlow.Engine
```

**预期输出**: 
```
正在还原 D:\Projects\AuditFlow\AuditFlow.Shared\AuditFlow.Shared.csproj...
正在还原 D:\Projects\AuditFlow\AuditFlow.Simulator\AuditFlow.Simulator.csproj...
正在还原 D:\Projects\AuditFlow\AuditFlow.Engine\AuditFlow.Engine.csproj...
还原成功。
```

### 步骤 6: 安装前端依赖

**重要**: 这一步**需要 Node.js** ✅

```powershell
cd AuditFlow.UI

# 安装 npm 依赖（需要 Node.js）
npm install
```

**预期输出**:
```
added 500+ packages in 30s
```

**如果遇到网络问题**:
```powershell
# 使用国内镜像（可选）
npm config set registry https://registry.npmmirror.com
npm install
```

---

## 🗄️ 数据库配置

### 步骤 7: 启动 SQL Server LocalDB

```powershell
# 启动 LocalDB 实例
sqllocaldb start mssqllocaldb

# 验证运行状态
sqllocaldb info mssqllocaldb
```

**预期输出**:
```
名称:               mssqllocaldb
版本:               15.0.2000.5
共享名:             
所有者:            你的用户名
自动创建:          是
状态:              正在运行
上次启动时间:      2026-01-20 10:30:00
实例管道名称:      np:\\.\pipe\LOCALDB#xxxxx\tsql\query
```

### 步骤 8: 验证数据库连接字符串

检查配置文件中的连接字符串是否正确：

**AuditFlow.Simulator\appsettings.json**:
```json
{
  "ConnectionStrings": {
    "DefaultConnection": "Server=(localdb)\\mssqllocaldb;Database=CS_SimulatorDB;Trusted_Connection=True;MultipleActiveResultSets=true"
  }
}
```

**AuditFlow.Engine\appsettings.json**:
```json
{
  "ConnectionStrings": {
    "DefaultConnection": "Server=(localdb)\\mssqllocaldb;Database=AuditFlowDB;Trusted_Connection=True;MultipleActiveResultSets=true"
  },
  "SimulatorApiUrl": "http://localhost:5001"
}
```

**注意**: 
- 如果 LocalDB 实例名不是 `mssqllocaldb`，需要修改连接字符串
- 检查实例名: `sqllocaldb info`

---

## 🚀 服务启动

### 步骤 9: 启动后端服务

#### 9.1 启动 CS_Simulator (终端 1)

打开 **PowerShell** 或 **命令提示符**:

```powershell
cd D:\Projects\AuditFlow\AuditFlow.Simulator
dotnet run
```

**等待输出**:
```
info: Microsoft.Hosting.Lifetime[14]
      Now listening on: http://localhost:5001
info: Microsoft.Hosting.Lifetime[0]
      Application started. Press Ctrl+C to shut down.
```

**首次启动会**:
- 自动创建数据库 `CS_SimulatorDB`
- 自动创建表结构
- 自动生成 50-100 条随机 CVE 威胁数据

#### 9.2 启动 AuditFlow_Engine (终端 2)

打开**新的** PowerShell 窗口:

```powershell
cd D:\Projects\AuditFlow\AuditFlow.Engine
dotnet run
```

**等待输出**:
```
info: Microsoft.Hosting.Lifetime[14]
      Now listening on: http://localhost:5002
info: Microsoft.Hosting.Lifetime[0]
      Application started. Press Ctrl+C to shut down.
```

**首次启动会**:
- 自动创建数据库 `AuditFlowDB`
- 自动创建表结构
- 从 Simulator 获取主机名并生成 5-10 条硬件资产数据

#### 9.3 启动前端 (终端 3)

**注意**: 这一步**需要 Node.js** ✅

打开**新的** PowerShell 窗口:

```powershell
cd D:\Projects\AuditFlow\AuditFlow.UI
npm run dev  # 需要 Node.js
```

**等待输出**:
```
  VITE v7.2.4  ready in 500 ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

---

## ✅ 验证测试

### 步骤 10: 测试服务

#### 10.1 测试 Simulator API

打开**新的** PowerShell 窗口:

```powershell
# 测试威胁摘要 API
Invoke-WebRequest -Uri "http://localhost:5001/api/v1/threats/summary" -UseBasicParsing | ConvertFrom-Json

# 应该返回 JSON 数据，包含 hostThreatCounts 等字段
```

#### 10.2 测试 Engine API

```powershell
# 测试审计摘要 API
Invoke-WebRequest -Uri "http://localhost:5002/api/audit/summary" -UseBasicParsing | ConvertFrom-Json

# 应该返回 JSON 数据，包含 complianceItems、totalAssets 等字段
```

#### 10.3 测试前端

1. 打开浏览器
2. 访问: **http://localhost:5173**
3. 应该看到 AuditFlow Dashboard，显示：
   - 总资产数
   - 合规/不合规统计
   - 设备列表

### 步骤 11: 检查数据库

#### 11.1 使用 SQL Server Management Studio (SSMS)

1. **打开 SSMS**
2. **连接到 LocalDB**:
   - 服务器名称: `(localdb)\mssqllocaldb`
   - 身份验证: Windows 身份验证
3. **查看数据库**:
   - 应该看到 `CS_SimulatorDB` 和 `AuditFlowDB`
4. **查看数据**:
   ```sql
   -- 查看威胁数据
   USE CS_SimulatorDB;
   SELECT COUNT(*) FROM CVEThreats;
   
   -- 查看硬件资产
   USE AuditFlowDB;
   SELECT * FROM HardwareAssets;
   ```

#### 11.2 使用命令行 (可选)

```powershell
# 使用 sqlcmd 连接
sqlcmd -S "(localdb)\mssqllocaldb" -Q "SELECT COUNT(*) FROM CS_SimulatorDB.dbo.CVEThreats"
sqlcmd -S "(localdb)\mssqllocaldb" -Q "SELECT COUNT(*) FROM AuditFlowDB.dbo.HardwareAssets"
```

---

## 🔄 使用启动脚本（可选）

### 创建启动脚本

创建 `start-all.ps1` 文件:

```powershell
# start-all.ps1
Write-Host "=== Starting AuditFlow Services ===" -ForegroundColor Cyan

# 启动 Simulator
Start-Process powershell -ArgumentList "-NoExit", "-Command", "cd '$PSScriptRoot\AuditFlow.Simulator'; Write-Host '=== CS_Simulator (Port 5001) ===' -ForegroundColor Green; dotnet run"

Start-Sleep -Seconds 5

# 启动 Engine
Start-Process powershell -ArgumentList "-NoExit", "-Command", "cd '$PSScriptRoot\AuditFlow.Engine'; Write-Host '=== AuditFlow_Engine (Port 5002) ===' -ForegroundColor Green; dotnet run"

Start-Sleep -Seconds 5

# 启动 Frontend
Start-Process powershell -ArgumentList "-NoExit", "-Command", "cd '$PSScriptRoot\AuditFlow.UI'; Write-Host '=== Frontend (Port 5173) ===' -ForegroundColor Green; npm run dev"

Write-Host "`n✓ All services starting..." -ForegroundColor Green
Write-Host "Frontend: http://localhost:5173" -ForegroundColor Cyan
```

**使用方法**:
```powershell
.\start-all.ps1
```

---

## 🐛 常见问题

### 问题 1: dotnet 命令未找到

**错误**: `'dotnet' 不是内部或外部命令`

**解决**:
1. 检查 .NET SDK 是否已安装: `dotnet --version`
2. 如果未安装，重新安装 .NET SDK
3. 重启 PowerShell/命令提示符
4. 检查环境变量 PATH 是否包含 .NET 路径

### 问题 2: npm 命令未找到

**错误**: `'npm' 不是内部或外部命令`

**解决**:
1. 检查 Node.js 是否已安装: `node --version`
2. 如果未安装，重新安装 Node.js
3. 重启 PowerShell/命令提示符
4. 检查环境变量 PATH 是否包含 Node.js 路径

### 问题 3: 数据库连接失败

**错误**: `Cannot open database "CS_SimulatorDB"`

**解决**:
1. 启动 LocalDB: `sqllocaldb start mssqllocaldb`
2. 检查连接字符串中的实例名是否正确
3. 检查 LocalDB 实例名: `sqllocaldb info`
4. 如果实例名不同，修改 `appsettings.json` 中的连接字符串

### 问题 4: 端口被占用

**错误**: `Address already in use` 或 `端口已被占用`

**解决**:
```powershell
# 检查端口占用
netstat -ano | findstr ":5001 :5002 :5173"

# 结束占用进程（替换 PID 为实际进程ID）
taskkill /PID <PID> /F

# 或修改端口（在 Program.cs 中）
builder.WebHost.UseUrls("http://localhost:5003");  # 改为其他端口
```

### 问题 5: CORS 错误

**错误**: `Access to fetch at 'http://localhost:5002/api/audit/summary' from origin 'http://localhost:5173' has been blocked by CORS policy`

**解决**:
1. 确保所有服务都已启动
2. 检查前端访问的端口是否为 5173
3. 检查后端 CORS 配置是否正确

### 问题 6: 前端空白页面

**解决**:
1. 打开浏览器开发者工具 (F12)
2. 查看 Console 标签的错误信息
3. 查看 Network 标签，检查 API 请求是否成功
4. 确保后端服务已完全启动（等待 30-60 秒）

### 问题 7: npm install 很慢或失败

**解决**:
```powershell
# 使用国内镜像
npm config set registry https://registry.npmmirror.com

# 清除缓存
npm cache clean --force

# 重新安装
npm install
```

---

## 📝 部署检查清单

迁移完成后，使用此清单验证：

- [ ] .NET SDK 已安装并验证 (`dotnet --version`)
- [ ] Node.js 已安装并验证 (`node --version`)
- [ ] SQL Server LocalDB 已安装并运行 (`sqllocaldb info mssqllocaldb`)
- [ ] 项目文件已完整复制
- [ ] .NET 依赖已还原 (`dotnet restore`)
- [ ] 前端依赖已安装 (`npm install`)
- [ ] Simulator 服务启动成功 (Port 5001)
- [ ] Engine 服务启动成功 (Port 5002)
- [ ] 前端服务启动成功 (Port 5173)
- [ ] Simulator API 测试通过
- [ ] Engine API 测试通过
- [ ] 前端页面正常显示
- [ ] 数据库数据已生成

---

## 🎯 快速启动命令

迁移完成后，可以使用以下命令快速启动：

```powershell
# 方式 1: 使用启动脚本
.\start-all.ps1

# 方式 2: 手动启动（3个终端窗口）
# 终端 1:
cd AuditFlow.Simulator && dotnet run

# 终端 2:
cd AuditFlow.Engine && dotnet run

# 终端 3:
cd AuditFlow.UI && npm run dev
```

---

## 📞 获取帮助

如果遇到问题：

1. 查看 `DevGuide.md` - 完整的技术文档
2. 查看 `ISSUE_LOGS.md` - 已知问题和解决方案
3. 检查服务窗口的错误信息
4. 查看浏览器控制台的错误信息

---

**最后更新**: 2026-01-20  
**版本**: 1.0.0
